---
const navItems = [
  { label: 'トップ', href: '/' },
  { label: 'メニュー', href: '/menu' },
  { label: 'アクセス', href: '/access' },
  { label: 'ご予約', href: '/reservation' },
];
---

<footer class="border-t border-border">
  <div class="max-w-6xl mx-auto px-6 py-16">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
      <!-- Logo & Description -->
      <div>
        <h3 class="font-serif text-2xl tracking-[0.15em] mb-2">華鹿</h3>
        <p class="font-display text-sm tracking-[0.2em] text-text-muted mb-4">KAROKU</p>
        <p class="text-sm text-text-muted leading-relaxed">
          昼は定食屋、夜は飲み屋。<br />
          奈良の美味しいお米と日本酒を<br />
          心ゆくまでお楽しみください。
        </p>
      </div>

      <!-- Navigation -->
      <div>
        <h4 class="text-sm tracking-[0.2em] text-accent mb-6">NAVIGATION</h4>
        <ul class="space-y-3">
          {navItems.map((item) => (
            <li>
              <a
                href={item.href}
                class="text-sm text-text-muted hover:text-accent transition-colors duration-300 tracking-[0.1em]"
              >
                {item.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Info -->
      <div>
        <h4 class="text-sm tracking-[0.2em] text-accent mb-6">INFORMATION</h4>
        <div class="space-y-3 text-sm text-text-muted">
          <div>
            <p class="tracking-[0.1em]">営業時間</p>
            <p>月〜土 11:30-14:00 / 17:00-21:00</p>
            <p>日・祝 11:30-14:00</p>
          </div>
          <div>
            <p class="tracking-[0.1em]">定休日</p>
            <p>不定休</p>
          </div>
          <div class="pt-4">
            <a
              href="https://www.instagram.com/"
              target="_blank"
              rel="noopener noreferrer"
              class="hover:text-accent transition-colors duration-300 tracking-[0.15em]"
            >
              Instagram
            </a>
          </div>
        </div>
      </div>
    </div>

    <div class="mt-16 pt-8 border-t border-border text-center">
      <p class="text-xs text-text-muted tracking-[0.15em]">
        &copy; {new Date().getFullYear()} 食堂 華鹿 -KAROKU- All Rights Reserved.
      </p>
    </div>
  </div>
</footer>
